import { StyleSheet } from "react-native";

const estilos = StyleSheet.create ({
    container: {
        flex:1,
        alignItems:"center"
    },
    header:{
        marginTop:80,
        fontSize:20,
        fontWeight:"bold",
        color:"purple"
    }
})

export default estilos